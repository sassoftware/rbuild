The rbuild rebase command now takes --interactive and --test arguments,
and tells not only about changes to the version of the product definition,
but also (and more importantly) changes to the versions of the elements
of the search path.
